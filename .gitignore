# Vim
.*.swp

# Debian live output files
*.iso

# Debian live generated files
.lock
*/config/binary
*/config/bootstrap
*/config/build
*/config/chroot
*/config/common
*/config/source
*/config/hooks/*/0*

/live-image-*

# Large includes that we don't want in git
/config/includes.chroot/opt/

# SSH keys that need to be unreadable for git
id_nagios
id_rsync

# Other SSH keys... it might be best if they are not on public github
id_polyproglive


# various other things
.cache
.vagrant
