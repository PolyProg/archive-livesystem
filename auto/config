#!/bin/sh

lb config noauto \
    --apt-indices false \
    --apt-recommends false \
    --bootappend-live "boot=live config quiet splash noprompt swapon noroot toram" \
    --clean \
    --distribution wheezy \
    --iso-application "PolyProg Live" \
    --iso-publisher "PolyProg" \
    --iso-volume "PolyProg Live" \
    --memtest none \
    --mirror-binary http://mirror.switch.ch/ftp/mirror/debian/ \
    "${@}"
